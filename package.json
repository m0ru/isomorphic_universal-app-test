{
  "name": "isomorphic_universal-app-test",
  "version": "0.0.1",
  "description": "Playing around a bit with server-side pre-rendering.",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p build && browserify -t babelify index.js -o build/compiled.js",
    "prestart": "npm run build",
    "start": "node build/compiled.js"
  },
  "author": "moru",
  "license": "MIT",
  "devDependencies": {
    "babel": "^6.1.18",
    "babel-cli": "^6.2.0",
    "babel-preset-es2016-node5": "^1.1.2",
    "babelify": "^7.2.0",
    "browserify": "^12.0.1"
  }
}
